<!DOCTYPE html>
<html lang="th">
  <head>
    <!--ตกแต่ง GUI-->
    <style>
      /*สีแบงค์กาวด์หัว*/
      .bg-header {
        background-color: #e0a2a2;
      }

      /*สีหน้าเว็บ*/
      body {
        font-size: 1rem;
        background-color: #f6f8fa;
      }

      /*ปรับตาราง*/
      th {
        line-height: 2rem;
      }

      /*ใส่คุณสมบัติของปุ่ม*/
      .button1 {
        border: none;
        color: white;
        padding: 10px 20px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 3px 2px;
        transition-duration: 0.4s;
        cursor: pointer;
        font-weight: bold;
      }

      .button2 {
        border: none;
        color: white;
        padding: 5px 13px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 3px 2px;
        transition-duration: 0.4s;
        cursor: pointer;
        font-weight: bold;
      }

      /*ตกแต่งปุ่ม*/
      .buttonstyle1 {
        background-color: white;
        color: black;
        border: 2px solid #e0a2a2;
      }

      /*เวลาเอาเมาส์ชี้ สีเปลี่ยน*/
      .buttonstyle1:hover {
        background-color: #e0a2a2;
        color: white;
      }

      /*ตกแต่งปุ่ม*/
      .buttonstyle2 {
        background-color: #e0a2a2;
        color: white;
      }

      /*เวลาเอาเมาส์ชี้ สีเปลี่ยน*/
      .buttonstyle2:hover {
        background-color: #e36060;
        color: white;
      }

      /*ทำให้อยู่ในระดับเดียวกัน*/
      .same-level {

        display: flex;
        flex-flow: row;
      }
    </style>

    <!-- ใส่ meta tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap ใช้ทำ Frontend Framework ให้ดูดี สวยงามขึ้น-->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x"
      crossorigin="anonymous"
    />

    <!-- ชื่อแท็บ -->
    <title><%- title %></title>
  </head>

  <body>
    <nav class="navbar navbar-dark bg-header">
      <div class="container-fluid">
        <a class="navbar-brand" href="/"
          >63050176&nbsp;&nbsp;|&nbsp;&nbsp;Rinrada Laiad</a>
      </div>
    </nav>
